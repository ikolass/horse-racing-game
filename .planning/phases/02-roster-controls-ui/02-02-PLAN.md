---
phase: 02-roster-controls-ui
plan: 02
type: execute
wave: 2
depends_on:
  - 02-01
files_modified:
  - src/App.vue
  - src/components/RaceTrackPlaceholder.vue
  - src/components/ResultsPlaceholder.vue
autonomous: false
requirements:
  - ROST-01
  - ROST-02
  - SCHED-01
  - SCHED-04
  - RACE-01
  - RACE-04

must_haves:
  truths:
    - "App.vue renders a 3-column grid with roster column, race track placeholder, and results placeholder"
    - "Roster column contains GameControls above HorseRoster"
    - "Center column shows 'Race Track' placeholder text"
    - "Right column shows 'Results' placeholder text"
    - "All 20 horses are visible in the browser with name, color swatch, and condition score"
    - "Generate button click creates schedule (verifiable in Vuex devtools) and enables Start"
    - "Start button click transitions gamePhase to RACING (verifiable in Vuex devtools)"
  artifacts:
    - path: "src/App.vue"
      provides: "Three-column game layout shell wiring all Phase 2 components"
      contains: "GameControls"
      min_lines: 20
    - path: "src/components/RaceTrackPlaceholder.vue"
      provides: "Center column empty state for Phase 3"
      contains: "Race Track"
    - path: "src/components/ResultsPlaceholder.vue"
      provides: "Right column empty state for Phase 4"
      contains: "Results"
  key_links:
    - from: "src/App.vue"
      to: "src/components/HorseRoster.vue"
      via: "import and render in roster column"
      pattern: "HorseRoster"
    - from: "src/App.vue"
      to: "src/components/GameControls.vue"
      via: "import and render above roster"
      pattern: "GameControls"
    - from: "src/App.vue"
      to: "src/components/RaceTrackPlaceholder.vue"
      via: "import and render in center column"
      pattern: "RaceTrackPlaceholder"
---

<objective>
Wire all Phase 2 components into App.vue's 3-column layout and create placeholder
components for the center (race track) and right (results) columns.

Purpose: Complete the Phase 2 UI — the player can see the roster and interact with
Generate/Start buttons. Placeholder columns reserve space for Phases 3 and 4.

Output: A working game shell where all 20 horses are visible, Generate creates a schedule,
and Start transitions gamePhase — all verifiable in browser + Vuex devtools.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-roster-controls-ui/02-CONTEXT.md
@.planning/phases/02-roster-controls-ui/02-UI-SPEC.md
@.planning/phases/02-roster-controls-ui/02-01-SUMMARY.md

<interfaces>
<!-- Components created in Plan 01 that this plan imports -->

From src/components/HorseRoster.vue:
- Default export: Vue SFC component
- Renders 20 horse rows from Vuex
- Has data-testid="horse-roster"

From src/components/GameControls.vue:
- Default export: Vue SFC component
- Renders Generate Schedule + Start Race buttons
- Has data-testid="btn-generate" and data-testid="btn-start"

From src/assets/main.css:
- #app has grid-template-columns: 20% 55% 25%
- height: 100vh, overflow: hidden

From src/assets/base.css:
- --color-bg-secondary: #16213e  (placeholder column background)
- --color-text-muted: #6b7280  (placeholder text)
- --color-divider: #2d3748  (column divider lines)
- --color-text-primary: #e0e0e0
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create placeholder components and rewrite App.vue</name>
  <files>src/components/RaceTrackPlaceholder.vue, src/components/ResultsPlaceholder.vue, src/App.vue</files>
  <read_first>
    - src/App.vue (current Vite scaffold to fully replace)
    - src/assets/main.css (confirm #app grid layout from Plan 01)
    - src/assets/base.css (confirm CSS token names from Plan 01)
    - src/components/HorseRoster.vue (confirm import path and component name)
    - src/components/GameControls.vue (confirm import path and component name)
    - .planning/phases/02-roster-controls-ui/02-UI-SPEC.md (Layout Contract, Placeholder Columns spec, Copywriting Contract)
  </read_first>
  <action>
    **RaceTrackPlaceholder.vue** — Minimal Vue 3 SFC:
    ```html
    <template>
      <div class="placeholder">
        <span class="placeholder-text">Race Track</span>
      </div>
    </template>

    <style scoped>
    .placeholder {
      display: flex;
      align-items: center;
      justify-content: center;
      height: 100%;
      background: var(--color-bg-secondary);
    }
    .placeholder-text {
      font-size: var(--font-body);
      font-weight: var(--font-weight-regular);
      color: var(--color-text-muted);
    }
    </style>
    ```
    No script block needed (pure presentational).

    **ResultsPlaceholder.vue** — Same structure, text is "Results":
    ```html
    <template>
      <div class="placeholder">
        <span class="placeholder-text">Results</span>
      </div>
    </template>

    <style scoped>
    .placeholder {
      display: flex;
      align-items: center;
      justify-content: center;
      height: 100%;
      background: var(--color-bg-secondary);
    }
    .placeholder-text {
      font-size: var(--font-body);
      font-weight: var(--font-weight-regular);
      color: var(--color-text-muted);
    }
    </style>
    ```

    **App.vue** — Full replacement of Vite scaffold. No `<script setup>` imports of HelloWorld/TheWelcome. New content:

    ```html
    <script setup>
    import GameControls from './components/GameControls.vue'
    import HorseRoster from './components/HorseRoster.vue'
    import RaceTrackPlaceholder from './components/RaceTrackPlaceholder.vue'
    import ResultsPlaceholder from './components/ResultsPlaceholder.vue'
    </script>

    <template>
      <div class="col col-roster" data-testid="col-roster">
        <GameControls />
        <HorseRoster />
      </div>
      <div class="col col-track">
        <RaceTrackPlaceholder />
      </div>
      <div class="col col-results">
        <ResultsPlaceholder />
      </div>
    </template>

    <style scoped>
    .col {
      height: 100vh;
      overflow: hidden;
    }

    .col-roster {
      display: flex;
      flex-direction: column;
      background: var(--color-bg-secondary);
      border-right: 1px solid var(--color-divider);
    }

    .col-track {
      border-right: 1px solid var(--color-divider);
    }

    .col-results {
      /* no left border — track column has right border */
    }
    </style>
    ```

    Key layout notes:
    - The `#app` grid is set in main.css (grid-template-columns: 20% 55% 25%)
    - App.vue renders 3 direct children (one per grid column)
    - Roster column is flex-column: GameControls on top, HorseRoster fills remaining space
    - Column dividers are 1px solid var(--color-divider) as right-borders
    - data-testid="col-roster" on the roster column per UI-SPEC contract
    - NO imports of HelloWorld, TheWelcome, or Vue logo — all scaffold references removed

    Delete scaffold component files that are no longer imported:
    - src/components/HelloWorld.vue
    - src/components/TheWelcome.vue
    - src/components/WelcomeItem.vue
    - src/components/icons/ directory (entire directory)

    These files are unused after App.vue replacement and will cause confusion if left in place.
  </action>
  <verify>
    <automated>cd C:/Users/Administrator/WebstormProjects/untitled && grep "GameControls" src/App.vue && grep "HorseRoster" src/App.vue && grep "RaceTrackPlaceholder" src/App.vue && grep "ResultsPlaceholder" src/App.vue && grep "data-testid=\"col-roster\"" src/App.vue && grep "Race Track" src/components/RaceTrackPlaceholder.vue && grep "Results" src/components/ResultsPlaceholder.vue && test ! -f src/components/HelloWorld.vue && echo "PASS"</automated>
  </verify>
  <acceptance_criteria>
    - App.vue imports GameControls, HorseRoster, RaceTrackPlaceholder, ResultsPlaceholder
    - App.vue does NOT import HelloWorld, TheWelcome, or reference logo.svg
    - App.vue template has exactly 3 `.col` divs as direct children
    - App.vue has `data-testid="col-roster"` on the roster column
    - App.vue `.col-roster` has `border-right: 1px solid var(--color-divider)`
    - App.vue `.col-roster` has `display: flex; flex-direction: column`
    - App.vue renders GameControls before HorseRoster inside col-roster
    - RaceTrackPlaceholder.vue contains text "Race Track"
    - RaceTrackPlaceholder.vue has `background: var(--color-bg-secondary)`
    - ResultsPlaceholder.vue contains text "Results"
    - ResultsPlaceholder.vue has `background: var(--color-bg-secondary)`
    - src/components/HelloWorld.vue does NOT exist (deleted)
    - src/components/TheWelcome.vue does NOT exist (deleted)
    - src/components/WelcomeItem.vue does NOT exist (deleted)
  </acceptance_criteria>
  <done>App.vue renders 3-column layout with roster (GameControls + HorseRoster), race track placeholder, and results placeholder; Vite scaffold components deleted</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Verify complete Phase 2 UI in browser</name>
  <files>src/App.vue</files>
  <action>
    Human verification of the complete Phase 2 UI. The executor starts the dev server
    with `npm run dev` and presents the verification checklist to the user.

    What was built:
    - Complete Phase 2 UI: 20-horse roster with color swatches and condition badges
    - Generate Schedule and Start Race buttons with gamePhase gating
    - 3-column layout with placeholder columns for Phase 3 and 4

    Verification steps for the user:
    1. Run `npm run dev` and open the app in browser (likely http://localhost:5173)
    2. Verify dark theme: background should be dark navy (#1a1a2e), not white
    3. Verify 3-column layout: narrow roster column left (~20%), wide center column (~55%), results column right (~25%)
    4. Verify roster column:
       - "Horses" heading visible at top
       - All 20 horses listed with: colored square swatch, name, condition number badge
       - Swatches should show distinct colors per horse
       - Condition badges have dark blue (#0f3460) background
    5. Verify controls above roster:
       - "Generate Schedule" button — should be red/accent colored (#e94560), clickable
       - "Start Race" button — should be grayed out/disabled (gamePhase is IDLE)
    6. Click "Generate Schedule":
       - Button should remain enabled
       - "Start Race" button should become enabled (red/accent color)
       - Open Vuex devtools: schedule.rounds should have 6 entries, each with horseIndices array of 10 items
       - Vuex devtools: race.gamePhase should now be "SCHEDULED"
    7. Click "Start Race":
       - Vuex devtools: race.gamePhase should transition to "RACING"
       - Both buttons should now be disabled (grayed, not-allowed cursor)
    8. Verify placeholder columns:
       - Center column shows "Race Track" in muted gray text, centered
       - Right column shows "Results" in muted gray text, centered
  </action>
  <verify>
    <automated>cd C:/Users/Administrator/WebstormProjects/untitled && npm run dev -- --host 2>&1 | head -5 || echo "Dev server starts"</automated>
  </verify>
  <acceptance_criteria>
    - User confirms 20 horses visible with name, color swatch, and condition badge
    - User confirms Generate button creates schedule and transitions to SCHEDULED
    - User confirms Start button transitions to RACING
    - User confirms button disabled states match gamePhase matrix
    - User confirms 3-column layout with placeholder columns
  </acceptance_criteria>
  <done>User types "approved" confirming all Phase 2 UI elements render and function correctly</done>
</task>

</tasks>

<verification>
Phase 2 complete when:
1. `npm run dev` shows the game UI with no console errors
2. All 20 horses visible with name, color swatch, and condition badge
3. Generate creates schedule (Vuex devtools shows 6 rounds) and transitions to SCHEDULED
4. Start transitions to RACING (Vuex devtools confirms)
5. Button gating follows the state/phase mapping table from UI-SPEC
6. All data-testid attributes present: btn-generate, btn-start, horse-roster, horse-row-{0-19}, col-roster
</verification>

<success_criteria>
- 3-column dark-themed layout renders correctly in browser
- 20 horses with name + color swatch + condition badge visible in roster
- Generate Schedule creates 6 rounds in Vuex and transitions gamePhase to SCHEDULED
- Start Race transitions gamePhase to RACING
- Buttons disabled/enabled per gamePhase state matrix
- Center and right columns show placeholder text
- No Vite scaffold remnants (no HelloWorld, no Vue logo)
</success_criteria>

<output>
After completion, create `.planning/phases/02-roster-controls-ui/02-02-SUMMARY.md`
</output>
