---
phase: 05-tests
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - vitest.config.js
  - playwright.config.js
  - tests/helpers/createTestStore.js
autonomous: true
requirements:
  - TEST-01
  - TEST-03

must_haves:
  truths:
    - "`package.json` exposes `npm run test` for Vitest and `npm run test:e2e` for Playwright"
    - "Vitest can resolve the `@/` alias used by app source files"
    - "Playwright starts the Vite app automatically and uses a timeout large enough for all six rounds to finish"
  artifacts:
    - path: "package.json"
      provides: "Test scripts and dev dependency registration"
      contains: "\"test\""
    - path: "vitest.config.js"
      provides: "Vitest config with alias resolution"
      contains: "defineConfig"
    - path: "playwright.config.js"
      provides: "Playwright config with webServer and timeout"
      contains: "webServer"
    - path: "tests/helpers/createTestStore.js"
      provides: "Reusable Vuex store factory for unit specs"
      contains: "createStore"
  key_links:
    - from: "vitest.config.js"
      to: "src/store/index.js"
      via: "alias and test runtime resolution"
      pattern: "alias"
    - from: "playwright.config.js"
      to: "package.json"
      via: "webServer launches npm run dev"
      pattern: "npm run dev"
---

<objective>
Create the missing test infrastructure so Phase 5 execution can add unit tests and browser E2E coverage without fighting setup issues.

Purpose: The repo currently has Vitest installed but no runnable scripts or config, and it does not include Playwright at all.
Output: Runnable Vitest and Playwright setup plus a shared test-store helper.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Add package scripts and installable config entry points</name>
  <files>package.json, vitest.config.js, playwright.config.js</files>
  <read_first>
    - package.json
    - vite.config.js
    - src/main.js
    - src/store/index.js
    - .planning/phases/05-tests/05-RESEARCH.md
  </read_first>
  <action>
Update `package.json` so scripts include exactly:

- `"test": "vitest"`
- `"test:run": "vitest run"`
- `"test:e2e": "playwright test"`

Add `@playwright/test` to `devDependencies`.

Create `vitest.config.js` using `defineConfig` from `vitest/config` and the Vite Vue plugin. Configure:
- `resolve.alias['@'] = fileURLToPath(new URL('./src', import.meta.url))`
- `test.environment = 'node'`
- `test.globals = true`
- `test.include = ['tests/unit/**/*.spec.js']`

Create `playwright.config.js` using `defineConfig` from `@playwright/test` with:
- `testDir: './tests/e2e'`
- `timeout: 60000`
- `use.baseURL = 'http://127.0.0.1:4173'`
- `use.headless = true`
- `webServer.command = 'npm run dev -- --host 127.0.0.1 --port 4173'`
- `webServer.url = 'http://127.0.0.1:4173'`
- `webServer.reuseExistingServer = !process.env.CI`

Do not add watch-mode flags to any script or config.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const pkg=JSON.parse(fs.readFileSync('package.json','utf8')); const scripts=['test','test:run','test:e2e']; const missing=scripts.filter(k=>!pkg.scripts||!pkg.scripts[k]); if(missing.length){console.error('Missing scripts',missing); process.exit(1)}; ['vitest.config.js','playwright.config.js'].forEach(f=>{ if(!fs.existsSync(f)){console.error('Missing',f); process.exit(1)} }); console.log('infra entry points present');"</automated>
  </verify>
  <acceptance_criteria>
    - `package.json` contains scripts `test`, `test:run`, and `test:e2e`
    - `package.json` contains `@playwright/test` in `devDependencies`
    - `vitest.config.js` exists and contains the `@` alias pointing at `./src`
    - `vitest.config.js` includes `tests/unit/**/*.spec.js`
    - `playwright.config.js` exists and contains `timeout: 60000`
    - `playwright.config.js` contains `webServer` with `npm run dev -- --host 127.0.0.1 --port 4173`
  </acceptance_criteria>
  <done>Test runner scripts and configuration exist and are wired to the project structure.</done>
</task>

<task type="auto">
  <name>Task 2: Add a reusable Vuex store test helper</name>
  <files>tests/helpers/createTestStore.js</files>
  <read_first>
    - src/store/index.js
    - src/store/modules/horses.js
    - src/store/modules/schedule.js
    - src/store/modules/race.js
    - src/store/modules/results.js
  </read_first>
  <action>
Create `tests/helpers/createTestStore.js` exporting a helper that builds a fresh Vuex store with the real `horses`, `schedule`, `race`, and `results` modules. The helper must:

- import `createStore` from `vuex`
- import all four real modules from `src/store/modules/*.js`
- return a new store instance each time
- optionally accept a callback or override object for preloading module state when tests need arranged data

Keep the helper minimal and deterministic. Do not mount Vue components in this file.
  </action>
  <verify>
    <automated>node -e "const fs=require('fs'); const p='tests/helpers/createTestStore.js'; if(!fs.existsSync(p)){console.error('Missing helper'); process.exit(1)}; const c=fs.readFileSync(p,'utf8'); ['createStore','horses','schedule','race','results'].forEach(s=>{ if(!c.includes(s)){console.error('Missing token',s); process.exit(1)} }); console.log('store helper present');"</automated>
  </verify>
  <acceptance_criteria>
    - `tests/helpers/createTestStore.js` exists
    - the helper imports `createStore` from `vuex`
    - the helper imports all four real Vuex modules
    - the helper returns a fresh store instance on each call
  </acceptance_criteria>
  <done>Unit specs have a shared, low-friction way to instantiate the real store.</done>
</task>

</tasks>

<verification>
- Run `npm run test -- --passWithNoTests` or equivalent Vitest dry run and confirm config loads without alias errors
- Run `npx playwright test --list` and confirm Playwright config parses and discovers the E2E directory
</verification>

<success_criteria>
- Vitest and Playwright have first-class scripts in `package.json`
- Both config files exist and point at the current repo layout
- Execution can start writing tests without additional setup decisions
</success_criteria>

<output>
After completion, create `.planning/phases/05-tests/05-01-SUMMARY.md`
</output>
