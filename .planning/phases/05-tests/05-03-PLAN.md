---
phase: 05-tests
plan: 03
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - tests/e2e/game-flow.spec.js
autonomous: false
requirements:
  - TEST-03

must_haves:
  truths:
    - "The E2E test opens the real app, clicks Generate, clicks Start, waits for all six round result cards, and asserts exactly six visible result entries"
    - "The E2E uses the existing `data-testid` selectors instead of brittle text-only selectors"
    - "The E2E timeout is high enough for the six-round animated race to finish without fixed sleeps"
  artifacts:
    - path: "tests/e2e/game-flow.spec.js"
      provides: "Full browser flow regression coverage"
      contains: "result-round-6"
  key_links:
    - from: "tests/e2e/game-flow.spec.js"
      to: "src/components/GameControls.vue"
      via: "generate/start selectors"
      pattern: "btn-generate"
    - from: "tests/e2e/game-flow.spec.js"
      to: "src/components/ResultsPanel.vue"
      via: "result card selectors"
      pattern: "result-round-6"
---

<objective>
Add a Playwright regression that verifies the complete browser flow from schedule generation through the final results panel state.

Purpose: This plan satisfies TEST-03 against the real running app.
Output: One stable Playwright spec covering the end-to-end game loop.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Author the full browser flow spec</name>
  <files>tests/e2e/game-flow.spec.js</files>
  <read_first>
    - playwright.config.js
    - src/components/GameControls.vue
    - src/components/RaceTrack.vue
    - src/components/ResultsPanel.vue
    - src/App.vue
    - .planning/phases/05-tests/05-RESEARCH.md
  </read_first>
  <action>
Create `tests/e2e/game-flow.spec.js` using `@playwright/test`.

The spec must:
- call `page.goto('/')`
- locate the Generate button via `[data-testid="btn-generate"]`
- locate the Start button via `[data-testid="btn-start"]`
- click Generate
- assert Start is enabled after schedule generation
- click Start
- wait for `[data-testid="result-round-1"]` through `[data-testid="result-round-6"]` to become visible
- assert the results panel `[data-testid="results-panel"]` contains exactly 6 visible round result cards

Use `await expect(locator).toBeVisible({ timeout: ... })` waits, not arbitrary `page.waitForTimeout(...)` sleeps, except for very small debugging-only pauses which must not remain in the final file.

Do not add extra app instrumentation unless execution proves the existing selectors are insufficient.
  </action>
  <verify>
    <automated>npx playwright test tests/e2e/game-flow.spec.js</automated>
  </verify>
  <acceptance_criteria>
    - `tests/e2e/game-flow.spec.js` exists
    - the spec uses `page.goto('/')`
    - the spec uses selectors `btn-generate`, `btn-start`, `results-panel`, and `result-round-6`
    - the spec waits for six round result cards to appear
    - the spec asserts exactly 6 result cards are visible in the results panel
    - `npx playwright test tests/e2e/game-flow.spec.js` exits 0
  </acceptance_criteria>
  <done>The full browser flow is covered by a stable regression test against the real Vite app.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Confirm the automated browser flow matches visible behavior</name>
  <files>n/a</files>
  <action>
Run the app manually with `npm run dev`, then:

1. Click Generate Schedule
2. Confirm Start becomes clickable
3. Click Start Race
4. Watch all six rounds complete
5. Confirm six round result cards appear in the results panel in chronological order

If the app visibly stalls or selectors pass while the UI is wrong, capture the mismatch before closing the phase.
  </action>
  <verify>Human confirms the E2E assertions match visible browser behavior.</verify>
  <acceptance_criteria>
    - a human observes six rounds complete in the browser
    - the results panel visibly matches the Playwright assertions
  </acceptance_criteria>
  <done>Automated E2E coverage is aligned with the real visible flow.</done>
</task>

</tasks>

<verification>
- Run `npx playwright test tests/e2e/game-flow.spec.js`
- Run `npx playwright test`
- Optionally run `npm run dev` and manually observe the same sequence
</verification>

<success_criteria>
- The real app can be driven end-to-end in Playwright
- Six sequential result entries are asserted at the end of the race
- The phase has browser-level regression coverage for the primary user flow
</success_criteria>

<output>
After completion, create `.planning/phases/05-tests/05-03-SUMMARY.md`
</output>
