---
phase: 05-tests
plan: 02
type: execute
wave: 2
depends_on:
  - 05-01
files_modified:
  - tests/unit/store/horses.spec.js
  - tests/unit/store/schedule.spec.js
  - tests/unit/store/results.spec.js
  - tests/unit/store/race.spec.js
  - tests/unit/utils/raceEngine.spec.js
autonomous: true
requirements:
  - TEST-01
  - TEST-02

must_haves:
  truths:
    - "Every mutation in `schedule`, `race`, and `results` has at least one passing test"
    - "Every store action in `schedule`, `race`, and `results` has at least one passing test"
    - "The fairness spec runs 50 simulations and asserts the highest-condition horse lands in the top 3 more than 30 times"
  artifacts:
    - path: "tests/unit/store/race.spec.js"
      provides: "Coverage for race mutations and timer-driven actions"
      contains: "vi.useFakeTimers"
    - path: "tests/unit/store/schedule.spec.js"
      provides: "Coverage for schedule mutation/action behavior"
      contains: "generateSchedule"
    - path: "tests/unit/store/results.spec.js"
      provides: "Coverage for result mutation/action behavior"
      contains: "addRoundResult"
    - path: "tests/unit/utils/raceEngine.spec.js"
      provides: "Statistical fairness regression coverage"
      contains: "50"
  key_links:
    - from: "tests/unit/store/race.spec.js"
      to: "src/store/modules/race.js"
      via: "real timer-driven action coverage"
      pattern: "onRoundComplete"
    - from: "tests/unit/utils/raceEngine.spec.js"
      to: "src/utils/raceEngine.js"
      via: "weighted finish-order fairness assertions"
      pattern: "computeFinishOrder"
---

<objective>
Author the Vitest unit suite that proves the Vuex layer and fairness logic behave correctly and remain regressions-safe.

Purpose: This plan satisfies TEST-01 and TEST-02 with fast deterministic specs.
Output: Passing unit specs for store mutations/actions and race-engine fairness.
</objective>

<tasks>

<task type="auto">
  <name>Task 1: Cover store modules with deterministic unit specs</name>
  <files>tests/unit/store/horses.spec.js, tests/unit/store/schedule.spec.js, tests/unit/store/results.spec.js, tests/unit/store/race.spec.js</files>
  <read_first>
    - tests/helpers/createTestStore.js
    - src/store/modules/horses.js
    - src/store/modules/schedule.js
    - src/store/modules/results.js
    - src/store/modules/race.js
    - src/config/gameConfig.js
  </read_first>
  <action>
Create four unit spec files under `tests/unit/store`.

`horses.spec.js` must assert:
- the module exposes 20 horses
- all horse names are unique
- all horse colors are unique
- every `condition` is between 1 and 100

`schedule.spec.js` must cover:
- `SET_ROUNDS` stores the passed array
- `CLEAR_SCHEDULE` resets rounds to `[]`
- `generateSchedule` creates exactly 6 rounds
- each round contains exactly 10 distinct horse indices
- round distances equal `[1200, 1400, 1600, 1800, 2000, 2200]`

`results.spec.js` must cover:
- `ADD_ROUND_RESULT` appends a result object
- `CLEAR_RESULTS` resets `rounds` to `[]`
- `addRoundResult` commits the payload shape unchanged
- `clearResults` empties the store

`race.spec.js` must cover:
- every race mutation (`SET_GAME_PHASE`, `SET_CURRENT_ROUND`, `SET_POSITIONS`, `RESET_POSITIONS`, `SET_INTERVAL_ID`)
- `transitionTo` accepts valid transitions and rejects invalid ones
- `startRace` sets round 1 and dispatches racing against the scheduled round
- `runRound` updates positions over fake timers and clears the interval when done
- `onRoundComplete` dispatches `results/addRoundResult`, transitions to `ROUND_COMPLETE`, and moves to `DONE` after round 6 or starts the next round otherwise
- `startRoundByNumber` reads the requested round and dispatches `runRound`

Use `vi.useFakeTimers()` in `race.spec.js` and advance timers instead of waiting in real time.
  </action>
  <verify>
    <automated>npm run test -- tests/unit/store</automated>
  </verify>
  <acceptance_criteria>
    - `tests/unit/store/horses.spec.js` exists and checks 20 unique horses with bounded conditions
    - `tests/unit/store/schedule.spec.js` checks both mutations and `generateSchedule`
    - `tests/unit/store/results.spec.js` checks both mutations and both actions
    - `tests/unit/store/race.spec.js` checks all five race mutations
    - `tests/unit/store/race.spec.js` uses `vi.useFakeTimers()`
    - `npm run test -- tests/unit/store` exits 0
  </acceptance_criteria>
  <done>Store behavior is covered by deterministic unit tests rather than manual inspection.</done>
</task>

<task type="auto">
  <name>Task 2: Add the race-engine fairness regression spec</name>
  <files>tests/unit/utils/raceEngine.spec.js</files>
  <read_first>
    - src/utils/raceEngine.js
    - .planning/REQUIREMENTS.md
    - .planning/phases/05-tests/05-RESEARCH.md
  </read_first>
  <action>
Create `tests/unit/utils/raceEngine.spec.js` that imports `computeFinishOrder` and defines a 10-horse field with one clearly highest-condition horse. Run exactly 50 simulations, count how many times that strongest horse appears in positions 0, 1, or 2, and assert the count is greater than 30.

Also add one shape-level assertion that `computeFinishOrder` returns a permutation of all horse indexes with no duplicates.

Do not mock `Math.random` in the fairness test. This spec must validate the actual weighted random behavior.
  </action>
  <verify>
    <automated>npm run test -- tests/unit/utils/raceEngine.spec.js</automated>
  </verify>
  <acceptance_criteria>
    - `tests/unit/utils/raceEngine.spec.js` exists
    - the file runs exactly 50 simulations
    - the strongest horse top-3 assertion is `> 30`
    - the file also asserts the finish order is a full permutation with no duplicates
    - `npm run test -- tests/unit/utils/raceEngine.spec.js` exits 0
  </acceptance_criteria>
  <done>The weighted-random race engine has a regression guard proving condition score materially affects outcomes.</done>
</task>

</tasks>

<verification>
- Run `npm run test -- tests/unit/store`
- Run `npm run test -- tests/unit/utils/raceEngine.spec.js`
- Run `npm run test` and confirm the full unit suite passes
</verification>

<success_criteria>
- Every Vuex mutation/action named in the current modules is covered by at least one unit test
- The fairness test passes consistently enough to trust the configured threshold
- Full Vitest run is green
</success_criteria>

<output>
After completion, create `.planning/phases/05-tests/05-02-SUMMARY.md`
</output>
